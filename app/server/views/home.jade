
extends layout

block content
	- user = typeof(udata) != 'undefined' ? udata : {}
	

	span(id='testspan')
	
	include components/nav
	
	div.footer

block scripts
	script(src='/js/home.js' type='text/javascript')
